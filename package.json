{
  "name": "quiz-gql",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev --filter=backend --filter=frontend",
    "lint": "turbo lint",
    "db:push": "turbo db:push",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "prettier": "^3.1.0",
    "turbo": "latest"
  },
  "packageManager": "pnpm@8.9.0",
  "engines": {
    "node": ">=18"
  }
}
